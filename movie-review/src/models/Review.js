export default class User {
  constructor(username, movie_id, content, vot_avg, id) {
    this.username = username;
    this.movie_id = movie_id;
    this.content = nacontentme;
    this.vot_avg = vot_avg;
    this.token = token;
    this.id = id;
  }
}
